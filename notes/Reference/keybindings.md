# Keybindings

The whole app is reachable from the home row.

## Navigation

| Key         | Action                        |
|-------------|-------------------------------|
| `j` / `↓`   | Move selection down           |
| `k` / `↑`   | Move selection up             |
| `g` / `G`   | Jump to top / bottom          |
| `l` / `↵`   | Expand folder                 |
| `h`         | Collapse folder / go to parent|

## Preview

| Key             | Action                    |
|-----------------|---------------------------|
| `J` / `Ctrl-d`  | Scroll preview down       |
| `K` / `Ctrl-u`  | Scroll preview up         |
| `Tab`           | Toggle zen mode           |

## Meta

| Key         | Action     |
|-------------|------------|
| `q` / `Esc` | Quit       |

> These mirror `todarchy` so muscle memory carries over between the two.
